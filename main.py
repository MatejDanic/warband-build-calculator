from build import Build
from attributes import attributes
from skills import skills, get_skill_base_attribute_index

build = Build('Matej', 'Noble', 'Male', 'Page', 'Student', 'Revenge')

print(build)
